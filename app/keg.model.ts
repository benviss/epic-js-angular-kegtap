export class Keg {
  public pintsLeft: number = 124;
  constructor(public brand: string, public name: string, public type: string, public bitterness: number, public alcohol: number, public price: number) {
  }
}
